input {
  beats {
    port => 5044
  }
}

output {
  if "app" in [tags] {
    elasticsearch {
      hosts => ["http://elasticsearch:9200"]
      index => "done-%{+YYYY.MM.dd}"
    }
  }
  stdout { codec => rubydebug }
}